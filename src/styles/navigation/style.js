export const navigationStyles = (theme) => ({
	headerStyle: {
		backgroundColor: theme.primaryBackground,
	},
	headerTintColor: theme.primaryNavigationTintColor,
	headerTitleStyle: {
		fontWeight: 'bold',
		fontSize: 24,
	},
	tabBarStyle: {
		backgroundColor: theme.primaryBackground,
	},
	tabBarActiveTintColor: theme.primaryActiveIconColor,
	tabBarInactiveTintColor: theme.primaryInactiveIconColor,
});
